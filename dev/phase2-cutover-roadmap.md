# Архив: Переход На Phase 2 По Умолчанию

> Статус: архивный документ. Переход завершён; файл хранится только как краткая историческая запись о rollout.

Статус: выполнено и закрыто.

Этот документ сохранён как архивная заметка о rollout-переходе. Текущее состояние кодовой базы и пользовательской документации теперь исходит из того, что:

- новые проекты получают `parallelRuntime.phase1Enabled=true` и `parallelRuntime.phase2Enabled=true`;
- отсутствие блока `parallelRuntime` нормализуется к тому же значению по умолчанию;
- `reason="phase2_disabled"` остаётся только для проектов, где `phase2Enabled=false` выставлен явно.

Актуальный источник истины:

- `README.md`
- `docs/cli/memory.md`
- `docs/cli/agents.md`
- `skills/madspec-cli-operator/`

Исторический смысл этого roadmap — зафиксировать, что rollout-переход больше не является открытой задачей из `dev`.
