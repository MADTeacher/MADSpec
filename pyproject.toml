[project]
name = "madspec-cli"
version = "0.9.94"
description = "CLI tool for MADSpec - experimental framework for software development using LLM agents"
requires-python = ">=3.11"
license = {text = "MIT"}
dependencies = [
    "typer",
    "rich",
    "huggingface_hub",
    "httpx[socks]",
    "lancedb",
    "numpy",
    "onnxruntime",
    "platformdirs",
    "readchar",
    "tokenizers",
    "truststore>=0.10.4",
]

[project.scripts]
madspec = "madspec_cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/madspec_cli"]
