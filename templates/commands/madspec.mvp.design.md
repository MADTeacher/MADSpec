---
description: MVP - Этап 1 - Создание интерактивного UI-прототипа на основе концепции проекта
handoffs:
  - label: Выбрать технологии
    agent: madspec.mvp.tech
    prompt: Выбери технологический стек на основе утвержденного UI-прототипа
---

## Пользовательский ввод

```text
$ARGUMENTS
```

## Обязательные skills

- Перед началом работы обязательно найди и прочитай skill `madspec-cli-operator`.
- Дальше работай, опираясь на `madspec-cli-operator` как на базовый operational layer для workflow `madspec.*`, branch-aware артефактов `.madspec/` и команд MADSpec CLI.
- Для visual/UI/UX design дополнительно обязательно найди и прочитай skill `frontend-design`.
- Используй `madspec-cli-operator` как базовый workflow/CLI skill, а `frontend-design` как основной design-skill.

Ты **ОБЯЗАН** учитывать пользовательский ввод перед продолжением (если он не пустой).

## Обязательный skill для visual design

- Применяй `frontend-design` как главный design-закон: типографика, цвет, атмосфера, уникальность. Всё, что там написано — обязательно.
- `.madspec/templates/ui-storyboard-contract.md` задает structural contract и review-навигацию, но **не задаёт** визуальный стиль.
- `madspec memory retrieve/capture/checkpoint/validate` остаются обязательным механизмом для canonical state.

## Правила диалога (обязательно)

- **Задавай вопросы строго по одному**: в каждом твоем сообщении должен быть **ровно 1 вопрос**, который требует ответа.
- **Не выдавай список вопросов заранее**.
- Если нужен выбор, задай **один вопрос** и предложи **не больше 3–5 вариантов**.
- **Дожидайся ответа** и только затем задавай следующий вопрос.
- Если ответ неполный — задай **один уточняющий вопрос**, а не несколько сразу.

## Structured Memory First (обязательно)

- Каноническое состояние хранится в `.madspec/<BRANCH>/memory/`.
- Реальные данные этапа design хранятся в `.madspec/<BRANCH>/memory/stages/mvp.design.json`.
- `.madspec/<BRANCH>/ui-design.md` и `project-context.md` считаются generated views и не редактируются вручную как источник истины.
- Перед началом и после каждого утвержденного блока по экрану, journey, навигации или платформенному ограничению используй `madspec memory retrieve --stage mvp.design --toon-output`, если этот контекст читает агент, и `madspec memory capture --stage mvp.design ...`.
- Используй design-specific flags, когда меняется canonical state: `--design-overview`, `--platform`, `--zone`, `--screen`, `--screen-feature`, `--flow`, `--flow-step`, `--flow-alternative`, `--nav`, `--platform-constraint`, `--screen-data`, `--next-action`.
- Приоритеты `P1/P2/P3` нужны для internal coverage через `--screen-feature`, но **не должны** появляться в user-facing prototype как бейджи или визуальные метки.
- Для этапа design **обязательно** заверши работу командой `madspec memory checkpoint --stage mvp.design ...`.
- Минимальный payload checkpoint:
  - `--summary` — итоговое решение по UI/UX;
  - `--fact/--decision` можно не дублировать, если они уже накоплены через `madspec memory capture --status validated`;
  - `--evidence` — ссылки на `.madspec/<BRANCH>/ui-design.md` и `.madspec/<BRANCH>/ui-prototype/index.html`.
- В обычных ходах диалога опирайся на `design_status`: какие обязательные поля еще пусты, какие concept-функции еще не покрыты экранами, каких prototype-файлов не хватает.
- Полный `artifact_state.design` запрашивай только перед финальной валидацией, итоговым обзором и `checkpoint`, используя `madspec memory retrieve --stage mvp.design --toon-output --full-artifact`, если этот вывод читает агент.
- **ОБЯЗАТЕЛЬНО**: для вызовов `madspec memory capture` и `madspec memory checkpoint` используй `--from-file`: записывай аргументы в JSON-файл и передавай путь через `--from-file <path>` (например, `madspec memory capture --from-file .madspec/.tmp/capture-args.json --json-output`). Ключи JSON соответствуют именам полей в `options` (например, `screens`, `flows`, `flow_steps`, `navigation`, `screen_data`), плюс `stage`, `branch`, `json_output`, `status` на верхнем уровне.

## Работа в нескольких чатах (обязательно)

- Для `mvp.design` многосессионная работа считается штатным сценарием.
- В начале **каждой новой сессии** сначала выполни `madspec memory retrieve --stage mvp.design --toon-output`, если этот вывод читает агент.
- После этого сверь:
  - `.madspec/<BRANCH>/ui-design.md`
  - `.madspec/<BRANCH>/ui-prototype/index.html`
  - остальные файлы в `.madspec/<BRANCH>/ui-prototype/`
  - `.madspec/templates/ui-storyboard-contract.md`
- Не опирайся на историю предыдущего чата как на источник истины, если она расходится с current structured memory и source artifacts.
- Не считай дизайн завершенным, пока пользователь явно не утвердил текущее состояние UI-прототипа.

## Изменил UI — проверь документацию (обязательно)

- Любое подтвержденное изменение экрана, flow, review-навигации, platform-specific поведения, состава данных на экране или связи экран ↔ функция должно обновлять не только HTML/CSS, но и canonical design memory.
- После изменения прототипов проверь, не устарели ли:
  - `.madspec/<BRANCH>/ui-design.md`
  - описание navigation
  - review journeys
  - coverage функций из concept
  - ссылки на prototype-файлы
- Рабочий порядок: сначала обнови source artifacts и typed design-state через `madspec memory capture`, затем пересобери generated views через `madspec memory checkpoint` или `madspec memory consolidate`, затем проверь консистентность через `madspec memory validate`.

## Описание

На этом этапе создается **интерактивный UI-прототип** пользовательского интерфейса на основе концепции проекта. Его задача — дать пользователю кликабельные экраны, которые **выглядят как настоящее приложение**, можно открыть в браузере и пройти по ключевым сценариям до старта реализации.

Прототип — не каталог функций и не описание экранов. Каждый HTML-файл должен выглядеть как реальный экран приложения с настоящими UI-компонентами, реалистичным контентом и уникальным дизайном под домен проекта.

Используй `.madspec/templates/ui-storyboard-contract.md` как structural contract. Этот файл задает структуру и review-навигацию, но **не задает** визуальный стиль. Визуальное решение агент должен разработать сам под домен проекта — уникальное для каждого проекта.

## Цель этапа

Создать кликабельный UI-прототип, который:

- решает проблемы, определенные в концепции;
- выглядит как реальное приложение с настоящими UI-компонентами и реалистичным контентом;
- дает пользователю возможность пройти ключевые сценарии по кликам;
- покрывает все concept features через internal design-state;
- позволяет утвердить финальное UI/UX-видение перед следующей стадией.

## Runtime workflow

0. **Определение текущей ветки**:
   - Перед началом работы определи текущую ветку через `madspec git current-branch`.
   - Все пути к артефактам используй в формате `.madspec/<BRANCH>/...`.
   - Если ни команда, ни файл недоступны, используй значение по умолчанию `main`.

1. **Загрузка контекста**:
   - Прочитай `.madspec/<BRANCH>/concept.md`.
   - Прочитай `.madspec/<BRANCH>/project-context.md`.
   - Загрузи `.madspec/templates/ui-storyboard-contract.md`.
   - Найди и прочитай skill `madspec-cli-operator`.
   - Найди и прочитай skill `frontend-design`.
   - Извлеки все concept features и ключевые пользовательские сценарии.
   - Если в concept есть authentication, onboarding, verification или другой access gate, используй его как начало primary review flow.

2. **Визуальное направление** *(выполняется ДО написания любого HTML — обязательно)*:
   - Применяя принципы `frontend-design` skill, определи уникальное визуальное решение для **этого конкретного проекта**:
     - **Эстетическое направление**: конкретное, нешаблонное описание (не "тёмный неон" — это запрещено). Например: "Editorial с экономией пространства, как в финансовом отчёте 1990-х", "Уютный кабинет с кожаными переплётами и тёплым светом", "Хирургическая чистота медицинского оборудования".
     - **Типографика**: ровно 2 шрифта из Google Fonts — один Display (для заголовков), один Body (для текста). Оба должны быть характерными и нешаблонными. Запрещены: Inter, Roboto, Arial, Helvetica, system-ui, sans-serif как единственный выбор.
     - **Цветовая палитра**: основной цвет, акцентный цвет, фоновый цвет — выбери уникальные для домена проекта. Запрещены как default: тёмный фон + cyan + pink.
     - **Ключевой визуальный мотив**: то, что запоминается и отличает этот дизайн от любого другого.
   - Покажи пользователю это решение в виде краткого дизайн-брифа.
   - Дождись подтверждения или правок перед вёрсткой.

3. **Определение review journeys**:
   - Выдели primary review flow и дополнительные journeys.
   - Для каждого flow определи entry screen, последовательность экранов и ожидаемый результат.
   - Сгруппируй связанные функции на одном экране только там, где это помогает review story.

4. **Создание HTML/CSS UI-прототипов**:
   - Каждый `[screen].html` должен **выглядеть как реальный экран приложения** — с настоящими UI-компонентами (nav-bar, sidebar, формы, списки, карточки с реальными mock-данными, кнопки действий).
   - Реалистичный fake-контент: имена, числа, даты, названия — не placeholder "Название элемента" или "Текст описания".
   - Подключи Google Fonts через `<link>` — те, что выбраны в шаге 2. Не используй системные шрифты.
   - Review-навигация (Back / Next / Home) реализуется как компактный `position: fixed` оверлей в углу экрана, чтобы **не занимать основное пространство** и не вытеснять контент приложения.
   - `index.html` — обзорная страница review с картой journeys, но оформленная в том же визуальном стиле, что и экраны приложения.
   - Допустим vanilla JavaScript для mock-navigation, state toggles и интерактивных состояний.
   - Прототип не требует реальной бизнес-логики, но обязан быть кликабельным по основным review journeys.

5. **Фиксация flows и экранов в memory**:
   - После утверждения каждого куска дизайна записывай `--screen`, `--screen-feature`, `--flow`, `--flow-step`, `--flow-alternative`, `--nav`, `--screen-data`.
   - Используй текущую модель `screens`, `flows`, `navigation` как canonical основу:
     - `flows[].steps` = порядок прохождения сценария;
     - первый step flow = entry screen journey;
     - первый flow = primary review flow.

6. **Локальный просмотр**:
   - Не спрашивай пользователя, чем запускать preview.
   - Выбери самый простой доступный static server в локальной среде.
   - Создай в `.madspec/<BRANCH>/ui-prototype/README.md` краткую инструкцию по запуску и открытию `index.html`.

7. **Валидация дизайна**:
   - Проверь, что все concept features покрыты экранами или экранными состояниями.
   - Проверь, что primary flow проходится кликами от `index.html` до финального экрана без ручного редактирования URL.
   - Проверь, что у экранов нет тупиков без review-навигации.
   - Проверь соответствие платформам, навигации и данным на экране.
   - Если validation не пройдена — не переходи к checkpoint.

8. **Checkpoint в structured memory**:
   - После явного approval пользователя выполни `madspec memory checkpoint --stage mvp.design`.
   - В `--evidence` укажи `.madspec/<BRANCH>/ui-design.md` и `.madspec/<BRANCH>/ui-prototype/index.html`.

## Требования к UI-прототипу

### Что должно быть на каждом экране

- Настоящий app-заголовок / nav-bar приложения (не review-шапка)
- Контент, специфичный для этого экрана: список элементов с реальными данными, форма с полями, карточки с именами/датами/числами, игровое поле, дашборд — то, что соответствует домену
- Реалистичный fake-контент — не "Пользователь 1", а "Алексей Морозов"; не "Сумма: X", а "14 820 ₽"
- Интерактивные элементы: кнопки, которые что-то показывают / скрывают / меняют состояние
- Компактный review overlay (Back / Next / Home) в углу — `position: fixed`, маленький, не вытесняет контент

### Что запрещено

**Запрещённые паттерны дизайна** (применяются ко всем проектам без разбора):
- Тёмный фон (#070916 и подобные) + neon cyan (#3df2ff) + neon pink (#ff48c4) как основная схема
- Glassmorphism (`backdrop-filter: blur`) как основной стиль всех панелей
- Arial Black / "Arial Narrow" uppercase как единственный шрифт заголовков
- Одинаковая `.topbar` с `.brand-mark` градиентным квадратом на каждом экране
- Метрика-карточки (`.metric-card`) как основной способ показать содержимое экрана

**Запрещённый контент на экранах**:
- Карточки с лейблами "UX-роль", "Primary CTA", "Обещание игроку", "Почему здесь есть..." — это внутренняя документация, не UI приложения
- Описание того, что экран "должен делать", вместо реального изображения того, что он делает
- Placeholder-текст вместо реалистичных данных

**Запрещённые шрифты** (без исключений):
- Inter, Roboto, Arial, Helvetica, system-ui как единственный выбор
- Использовать только Google Fonts, подключённые через `<link>`

## Важные принципы

- **ДЕЛАЙ реальный UI, а не документацию о нём**: каждый HTML-файл — это экран приложения, не описание экрана.
- **КАЖДЫЙ проект уникален визуально**: дизайн под домен, а не копия прошлого прототипа.
- **REVIEW-НАВИГАЦИЯ — вспомогательный инструмент**, а не главный элемент страницы. Fixed overlay в углу.
- **НЕ ПОКАЗЫВАЙ `P1/P2/P3`** в user-facing прототипе: приоритеты нужны для internal coverage, а не для визуального согласования.
- **ПРОСИ пользователя пройти кликабельный сценарий в браузере** и явно подтвердить соответствие ожиданиям.
- **НЕ ПЕРЕХОДИ** к следующему этапу без утверждения дизайна.

## Выходные артефакты

- `.madspec/templates/ui-storyboard-contract.md` - structural contract для UI-прототипов
- `.madspec/<BRANCH>/ui-prototype/` - директория с HTML/CSS UI-прототипами
  - `index.html` - обзорная review-страница с картой journeys
  - `[screen-name].html` - отдельные экраны, выглядящие как реальное приложение
  - `styles.css` - уникальные стили под домен проекта
  - `README.md` - инструкция по запуску
- `.madspec/<BRANCH>/memory/stages/mvp.design.json` - canonical design state
- `.madspec/<BRANCH>/ui-design.md` - generated artifact дизайна интерфейса
- `.madspec/<BRANCH>/project-context.md` - generated view контекста проекта

## Следующий этап

После утверждения дизайна перейди к `/madspec.mvp.tech` для выбора технологического стека.
